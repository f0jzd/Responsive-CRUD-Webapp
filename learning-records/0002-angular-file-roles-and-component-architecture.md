# Understanding Angular File Roles and Component Architecture

The user explored the anatomy of Angular and how files are structured in a Single-Page Application (SPA). Established the distinction between components (`*.component.ts` for UI and state), services (`*.service.ts` for backend HTTP data exchange), test files (`*.spec.ts`), and routing (`app.routes.ts`). In future sessions, component examples can reference `BookListComponent` as a concrete anchor.
